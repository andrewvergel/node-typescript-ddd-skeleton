const dbUser = 'default';
const dbPassword = 'toor';
const dbHost = 'localhost';
const dbPort = '6379';

export default {
  username: dbUser,
  password: dbPassword,
  host: dbHost,
  port: dbPort
};
