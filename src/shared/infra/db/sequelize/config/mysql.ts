const dbName = 'db_clean';
const dbUser = 'root';
const dbHost = 'localhost';
const dbDriver = 'mariadb';
const dbPassword = 'toor';

export default {
  database: dbName,
  username: dbUser,
  password: dbPassword,
  host: dbHost,
  dialect: dbDriver,
  models: []
};
